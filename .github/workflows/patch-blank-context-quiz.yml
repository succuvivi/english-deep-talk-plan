name: Patch blank context quiz

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/patch-blank-context-quiz.yml"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Patch blank context quiz
        run: python tools/patch_blank_context_quiz.py
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if ! git diff --cached --quiet; then
            git commit -m "hide quiz answers behind blank context"
            git push
          fi
