name: Fix word audio

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/fix-word-audio.yml"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Make word buttons speak only the word
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          s = p